<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dyz.myBatis.dao.FriendsMapper">
	<insert id="insertFriends" parameterType="com.dyz.gameserver.pojo.FindUser"
		useGeneratedKeys="true" keyProperty="id">
		<!-- WARNING - @mbggenerated This element is automatically generated by 
			MyBatis Generator, do not modify. -->
		insert into friends
		<trim prefix="(" suffix=")" suffixOverrides=",">
			<if test="followeruuid != null">
				followeruuid,
			</if>
			<if test="Befolloweruuid != null">
				Befolloweruuid,
			</if>
			<if test="createtime != null">
				createtime,
			</if>
		</trim>
		<trim prefix="values (" suffix=")" suffixOverrides=",">
			<if test="followeruuid != null">
				#{followeruuid,jdbcType=INTEGER},
			</if>
			<if test="Befolloweruuid != null">
				#{Befolloweruuid,jdbcType=INTEGER},
			</if>
			<if test="createtime != null">
				#{createtime,jdbcType=VARCHAR},
			</if>
		</trim>
	</insert>
	
	
	
	<select id="selectFindUserByBefolloweruuid" parameterType="com.dyz.gameserver.pojo.FindUser"
		resultType="com.dyz.gameserver.pojo.FindUser">
		select
		*
		from friends
		 where Befolloweruuid = #{Befolloweruuid}  AND followeruuid = #{followeruuid}
	</select>
	
	<select id="selectGetUser" parameterType="com.dyz.gameserver.pojo.PageModel"
		resultType="com.dyz.gameserver.pojo.Account">
		select
		account.*
		from account,friends WHERE account.uuid=friends.Befolloweruuid AND friends.followeruuid=#{keywords}  LIMIT
		#{begin},#{pageSize}
	</select>
	<select id="selectGetUserNum" parameterType="com.dyz.gameserver.pojo.PageModel" 
	resultType="java.lang.Integer">
		SELECT count(*)
		FROM
		account,friends
		WHERE account.uuid=friends.Befolloweruuid AND friends.followeruuid=#{keywords}
	</select>
</mapper>